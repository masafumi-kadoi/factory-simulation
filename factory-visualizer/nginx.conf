server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # No caching for HTML/JS/CSS during development
    add_header Cache-Control "no-store, no-cache, must-revalidate";

    location / {
        try_files $uri $uri/ /index.html;
    }
}
